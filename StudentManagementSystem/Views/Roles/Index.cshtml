@model IEnumerable<StudentManagementSystem.Models.Role>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Role_Tittle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role_Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Role_Tittle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role_Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
